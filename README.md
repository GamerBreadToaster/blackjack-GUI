# Blackjack
Welcome to my blackjack project! To start just download the code.

After you have to download some modules. You can do this by opening an terminal in this folder and pasting this command.

windows:
`py -m pip install -r requirements.txt`

linux:
`python3 -m pip install -r requirements.txt`
